// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firbaseConfig: {
  	apiKey: "AIzaSyAX3dPCSghKrLK6Cjg4QA6m9MEKLEEI36c",
    authDomain: "phonebook-dc2e6.firebaseapp.com",
    databaseURL: "https://phonebook-dc2e6.firebaseio.com",
    projectId: "phonebook-dc2e6",
    storageBucket: "phonebook-dc2e6.appspot.com",
    messagingSenderId: "681834558654"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
